{
  "name": "@vizteck/e2e",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "echo 'e2e tests skipped in unit test run — use pnpm test:e2e with apps running'",
    "test:e2e": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@types/node": "^20.19.43"
  }
}
