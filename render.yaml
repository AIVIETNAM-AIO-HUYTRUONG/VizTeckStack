services:
  - type: web
    name: vizteckstack-api
    runtime: node
    plan: free
    region: singapore
    buildCommand: pnpm install --frozen-lockfile && pnpm --filter @vizteck/db build && pnpm --filter @vizteck/api-gateway build
    startCommand: node apps/api-gateway/dist/main.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: ADMIN_TOKEN
        sync: false
